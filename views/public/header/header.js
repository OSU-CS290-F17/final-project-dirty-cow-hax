const newUserButton = document.getElementById('new-user-button');
const signInButton  = document.getElementById('sign-in-button');

const signInModal   = document.getElementById('user-info-modal')

newUserButton.addEventListener('click', (event) => {
    
    //Open a modal up asking for information
    //Pass information to server via POST
    //That should redirect to the main page

    

});
